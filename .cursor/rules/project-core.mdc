---
description: 固定资产质检 Agent 全局项目约定
alwaysApply: true
---

# 固定资产质检 Agent 全局约定

- **先答后改（默认）**：先回答问题、说明原因与拟改方案；**用户确认后再**修改代码/文档/配置；**`git commit`/`git push` 前须列清单并确认**。未经确认不直接改仓库、不默认提交/推送。详见 [docs/agent-collaboration.md](../../docs/agent-collaboration.md)。
- 新任务开始前，先阅读 `AGENTS.md`、`docs/agent-collaboration.md`、`docs/handoff/latest.md` 和 `docs/PROJECT_STRUCTURE.md`。
- 领域字段和业务口径以 `docs/domain-glossary.md` 为准。
- 架构和模块边界以 `docs/architecture.md` 为准。
- 质检结论只能使用：`PASS`、`WARN`、`FAIL`、`NEED_REVIEW`。
- 不提交真实资产编号、真实部门、真实人员、合同、发票或密钥信息。
- **`.env` / `FA_QC_LLM_API_KEY` 永不进 Git**（见 `docs/data-security.md`）；提交前运行 `python scripts/check_staged_no_secrets.py`；仅 `.env.example` 可入库。
- 长期有效的决策不要只留在聊天里，应写入 `docs/`、`.cursor/rules/` 或 `.cursor/skills/`。
- **LLM 优先级**：质检点准确 = `rules` + `ingest`（P0）；LLM 主战场 = `--llm-rules` / `--llm-checklist`（见 `docs/llm-agent-roadmap.md`）；`--llm` 报告叙述为低优先级。
